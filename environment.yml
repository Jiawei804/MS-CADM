name: MS-CADM
channels:
  - pytorch
  - nvidia
dependencies:
  - python >= 3.8
  - pytorch >= 1.13
  - torchvision
  - pytorch-cuda=11.7
  - pip:
    - timm
    - diffusers
    - accelerate
    - numpy
    - scipy
    - tqdm
    - jinja2
    - pandas
    - matplotlib

